<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:id="@+id/line_chart_layout"
        android:layout_width="match_parent"
        android:layout_height="160dp">

        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/line_chart"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <View
            android:id="@+id/circle_bg"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_centerInParent="true"
            android:visibility="invisible"
            android:background="@drawable/circle_highlighter_bg" />

        <View
            android:id="@+id/circle"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_centerInParent="true"
            android:visibility="invisible"
            android:background="@drawable/circle_highlighter" />
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/marqueeview_container"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_below="@id/line_chart_layout"
        android:layout_centerVertical="true"
        android:background="#80dcdfff"
        android:layout_marginTop="10dp">

    </RelativeLayout>

</RelativeLayout>